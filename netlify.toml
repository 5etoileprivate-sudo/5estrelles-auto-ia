[build]
  command = "npm run build"
  publish = ".next"

# The Next.js essential plugin is automatically installed by Netlify, 
# but declaring it ensures the build plugin compiles serverless functions correctly.
[[plugins]]
  package = "@netlify/plugin-nextjs"
